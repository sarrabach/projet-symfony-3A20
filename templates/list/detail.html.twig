{% extends 'base.html.twig' %}

{% block title %}Hello StudentController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello Details Page  ! ✅</h1>
        <ul>

        {% for formation in formations %}
         
           
        {% if formation.ref is same as(ref) %}

        <li> the ref is {{ formation.ref }}</li>
        <li> the title is  {{ formation.Titre }}</li>
        <li> the description is  {{ formation.Description }}</li>
        <li> the date is  {{ formation.date_debut }}</li>
        <li> the nbre of participant is  {{ formation.nb_participants }}</li>


{% endif %}
{% endfor %}

</ul>
</div>
{% endblock %}